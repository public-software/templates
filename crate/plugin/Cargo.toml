[package]
name = "{{CRATE}}"
description = "The {{COMPONENT}} plugin of {{REPO}}: a {{WIT_NAMESPACE}}:core component"
readme = "README.md"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true

# cdylib for the component build (wasm32-wasip2, through the plugin-runtime tooling), rlib for the host tests.
[lib]
crate-type = ["cdylib", "rlib"]

[lints]
workspace = true
